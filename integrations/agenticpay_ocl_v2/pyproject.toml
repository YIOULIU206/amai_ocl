[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "agenticpay-ocl-v2"
version = "0.1.0"
description = "Independent adaptive organizational control runtime for AgenticPay"
requires-python = ">=3.10"
dependencies = []

[tool.setuptools.packages.find]
where = ["src"]
